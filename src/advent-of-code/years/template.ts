// WARN: This is a template; Make sure to override the values
import { getPathToProblemInput } from "@/utils/advent-of-code";
import { read } from "@/utils/file-io";

const content = await read(getPathToProblemInput("<year>", "<day>"));

function solve(part: 1 | 2) {
	switch (part) {
		case 1:
			break;
		case 2:
			break;
	}
}

solve(1);
solve(2);
